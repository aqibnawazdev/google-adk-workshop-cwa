---
phase: 02-function-calling-and-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workshop-materials/02-tools-functions.ipynb
autonomous: true

must_haves:
  truths:
    - "Participant can complete TODO sections to implement search_flights function"
    - "Participant can complete TODO sections to implement search_hotels function"
    - "Participant can add tools to agent and test function calling"
    - "Participant understands when to use tools vs RAG (decision framework)"
    - "Participant sees tool invocation with debug output"
  artifacts:
    - path: "workshop-materials/02-tools-functions.ipynb"
      provides: "Exercise 2 notebook with function calling exercises"
      min_lines: 400
      contains: "search_flights"
  key_links:
    - from: "workshop-materials/02-tools-functions.ipynb"
      to: "google.adk.agents.Agent"
      via: "tools parameter"
      pattern: "tools=\\["
---

<objective>
Create the Exercise 2 notebook teaching function calling and tool use in ADK.

Purpose: Participants learn to give agents real-time data access through Python functions, understand the tools vs RAG decision framework, and implement error-in-context patterns.

Output: Complete 02-tools-functions.ipynb with TODO-guided exercises following 01-hello-agent.ipynb patterns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-function-calling-and-tools/02-RESEARCH.md
@workshop-materials/01-hello-agent.ipynb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 02-tools-functions.ipynb with exercise structure</name>
  <files>workshop-materials/02-tools-functions.ipynb</files>
  <action>
Create a Jupyter notebook following the 01-hello-agent.ipynb structure with these sections:

**Cell structure (approximately 18-20 cells):**

1. **Title markdown** - "Exercise 2: Function Calling & Tools" with learning objectives and 20min estimate

2. **Concept introduction markdown** - "What is Function Calling?" explaining:
   - Tools vs RAG decision framework (the KEY insight)
   - Table showing: Tools = real-time (flights, hotels), RAG = static (destination guides)
   - The key question: "Does this data change while user is talking?"

3. **Quick Setup code cell** - Same pattern as 01-hello-agent (PROJECT_ID, auth, pip install)

4. **Exercise 2A intro markdown** - "Implement search_flights"

5. **Exercise 2A code cell** - search_flights TODO skeleton:
   ```python
   from datetime import datetime
   from typing import Optional

   def search_flights(
       # TODO: Add parameters with type hints
       # origin: str - Airport code like 'SFO'
       # destination: str - Airport code like 'NRT'
       # departure_date: str - Date in YYYY-MM-DD
       # passengers: int = 1
   ) -> dict:
       """
       TODO: Write docstring with Args and Returns sections

       Hint: Include example values in Args (e.g., 'SFO', 'NRT', '2026-03-15')
       """
       # Debug output - shows when function is called
       print(f"search_flights called: {origin} -> {destination}")

       # TODO: Return mock flight data
       # Return structure:
       # {
       #     "status": "success",
       #     "flights": [
       #         {"airline": "United", "price": 850, "departure": "08:30", "arrival": "14:30+1"},
       #     ],
       #     "currency": "USD",
       #     "route": f"{origin} -> {destination}",
       #     "date": departure_date,
       # }
       pass
   ```

6. **Checkpoint markdown** - Test search_flights with expected output

7. **Exercise 2B intro markdown** - "Implement search_hotels"

8. **Exercise 2B code cell** - search_hotels TODO skeleton (similar structure)

9. **Exercise 2C intro markdown** - "Error Handling: Errors-in-Context Pattern"
   - Key pattern: Return error dicts, DON'T raise exceptions
   - Side-by-side comparison showing why LLM needs to see errors

10. **Exercise 2C code cell** - Add validation to search_flights:
    ```python
    # TODO: Add date validation
    # If date format is wrong, return:
    # {"status": "error", "error_message": "Invalid date format...", "example": "2026-03-15"}
    ```

11. **Exercise 2D intro markdown** - "Add Tools to Agent"

12. **Exercise 2D code cell** - Agent creation with tools parameter:
    ```python
    from google.adk.agents import Agent

    agent = Agent(
        model='gemini-2.5-flash',
        name='travel_assistant',
        description='Travel booking assistant with flight and hotel search',
        instruction='''You are a travel booking assistant.

        Use search_flights() and search_hotels() to help users plan trips.
        When a user mentions budget, filter results accordingly.
        If a tool returns an error, help the user fix their request.
        ''',
        tools=[
            # TODO: Add your functions here
            # search_flights,
            # search_hotels,
        ],
    )
    ```

13. **Exercise 2E intro markdown** - "Test Function Calling"

14. **Exercise 2E code cell** - Test queries showing tool invocation:
    ```python
    test_queries = [
        "Find flights from San Francisco to Tokyo on March 15",
        "Now find a hotel in Tokyo for March 15-20",
    ]

    for query in test_queries:
        print(f"\nYou: {query}")
        response = agent.generate_content(query)
        print(f"Agent: {response.text}")
    ```

15. **Budget filtering intro markdown** - "Exercise 2F: Budget Filtering" (AGENT-08)

16. **Budget filtering code cell** - Add max_price parameter, test with budget constraint

17. **Key takeaways markdown** - Summary of what was learned

18. **Solutions cell** - Complete implementations (collapsed/hidden)

19. **What's next markdown** - Preview of Exercise 3 (RAG integration)

**Patterns to follow from 01-hello-agent.ipynb:**
- Use `@title` for collapsible solution cells
- HTML comments for instructor notes: `<!-- INSTRUCTOR: ... -->`
- Checkpoint sections after each exercise with troubleshooting
- Time estimates in markdown headers
- Agent diagram similar to Exercise 1

**Requirements covered:**
- WORK-03: Exercise 2 function calling
- AGENT-01: Flight search
- AGENT-02: Hotel search
- AGENT-06: Error handling
- AGENT-08: Budget filtering
- INFRA-01: Mock flight API
- INFRA-02: Mock hotel API
- CONTEXT-01: Tools vs RAG decision framework
- CONTEXT-02: Real-time data access pattern
- CONTEXT-06: Error-in-context pattern
- CONTEXT-07: Cache-friendly (deterministic mock data)
  </action>
  <verify>
    - File exists: workshop-materials/02-tools-functions.ipynb
    - Cell count: 18-20 cells
    - Contains: "search_flights", "search_hotels", "Tools vs RAG", "error", "budget"
    - Follows ipynb JSON structure (valid notebook)
  </verify>
  <done>
    02-tools-functions.ipynb exists with complete TODO-guided exercise structure teaching function calling, error handling, budget filtering, and tools vs RAG decision framework
  </done>
</task>

</tasks>

<verification>
1. Notebook opens in Jupyter/Colab without errors
2. Cell structure matches 01-hello-agent.ipynb patterns
3. TODOs are clear and completable by participants
4. Decision framework (tools vs RAG) is prominent
5. Error-in-context pattern is explicitly demonstrated
6. Budget filtering exercise exists (AGENT-08)
</verification>

<success_criteria>
- 02-tools-functions.ipynb exists with 18-20 cells
- TODO sections guide participants through search_flights, search_hotels, error handling
- Tools vs RAG decision framework explained upfront
- Agent configured with tools parameter
- Budget filtering demonstrated
- Follows 01-hello-agent.ipynb patterns (structure, instructor notes, checkpoints)
</success_criteria>

<output>
After completion, create `.planning/phases/02-function-calling-and-tools/02-01-SUMMARY.md`
</output>
