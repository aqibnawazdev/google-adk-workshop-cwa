---
phase: 02-function-calling-and-tools
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - workshop-materials/reference-implementation/README.md
autonomous: true

must_haves:
  truths:
    - "README explains tools vs RAG decision framework"
    - "README documents available tools and their parameters"
    - "README shows example tool invocation output"
  artifacts:
    - path: "workshop-materials/reference-implementation/README.md"
      provides: "Updated documentation with tools section"
      contains: "Tools vs RAG"
  key_links:
    - from: "workshop-materials/reference-implementation/README.md"
      to: "workshop-materials/reference-implementation/tools.py"
      via: "documentation reference"
      pattern: "tools\\.py"
---

<objective>
Update reference implementation README to document the tool functions and decision framework.

Purpose: Provide participants with clear documentation explaining when to use tools vs RAG, what each tool does, and how to use them.

Output: Updated README.md with tools section explaining function calling patterns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-function-calling-and-tools/02-RESEARCH.md
@.planning/phases/02-function-calling-and-tools/02-01-SUMMARY.md
@.planning/phases/02-function-calling-and-tools/02-02-SUMMARY.md
@workshop-materials/reference-implementation/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README with tools documentation</name>
  <files>workshop-materials/reference-implementation/README.md</files>
  <action>
Read the existing README.md and add a new section documenting the tools. Insert after the existing content but before any "Running" or "Testing" sections.

**Add this section:**

```markdown
## Tools vs RAG: Decision Framework

This agent demonstrates the key decision: **when to use function calling vs knowledge retrieval**.

| Use Case | Approach | Why |
|----------|----------|-----|
| Flight availability | **Tool** (search_flights) | Data changes every minute |
| Hotel pricing | **Tool** (search_hotels) | Inventory updates constantly |
| Destination guides | **RAG** (Exercise 3) | Static content, rarely changes |
| Visa requirements | **RAG** (Exercise 3) | Updated infrequently |

**The Key Question:** Does this data change while the user is talking to the agent?
- **YES** -> Use a Tool (function calling)
- **NO** -> Use RAG (knowledge retrieval)

## Available Tools

### search_flights

Search for available flights between airports.

**Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| origin | str | Yes | Departure airport code (SFO, LAX, JFK) |
| destination | str | Yes | Arrival airport code (NRT, CDG, LHR) |
| departure_date | str | Yes | Date in YYYY-MM-DD format |
| passengers | int | No | Number of passengers (default: 1) |
| max_price | int | No | Maximum price per person in USD |

**Available Routes:** SFO->NRT, LAX->CDG, JFK->LHR

**Example Response:**
```json
{
  "status": "success",
  "flights": [
    {"airline": "United Airlines", "flight_number": "UA837", "price": 850, ...}
  ],
  "route": "SFO -> NRT",
  "currency": "USD"
}
```

### search_hotels

Search for available hotels in a destination.

**Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| location | str | Yes | City name (Tokyo, Paris, London) |
| check_in | str | Yes | Check-in date in YYYY-MM-DD format |
| check_out | str | Yes | Check-out date in YYYY-MM-DD format |
| guests | int | No | Number of guests (default: 1) |
| max_price_per_night | int | No | Maximum price per night in USD |

**Available Locations:** Tokyo, Paris, London

**Example Response:**
```json
{
  "status": "success",
  "hotels": [
    {"name": "Park Hyatt Tokyo", "stars": 5, "price_per_night": 450, ...}
  ],
  "location": "Tokyo",
  "nights": 5
}
```

## Error Handling Pattern

Tools return error information in the response (not exceptions) so the LLM can help users fix issues:

```json
{
  "status": "error",
  "error_message": "Invalid date format: 'march 15'. Use YYYY-MM-DD format.",
  "example": "2026-03-15"
}
```

This "errors-in-context" pattern lets the agent:
- Explain what went wrong
- Suggest how to fix it
- Offer alternatives

## File Structure

```
reference-implementation/
├── agent.py      # Agent definition and configuration
├── tools.py      # Tool function implementations (NEW)
├── README.md     # This file
└── .env.template # Environment configuration
```
```

Preserve the existing content (overview, architecture, running instructions) and integrate this new section logically.
  </action>
  <verify>
    - File updated: workshop-materials/reference-implementation/README.md
    - Contains "Tools vs RAG" section
    - Contains "search_flights" and "search_hotels" documentation
    - Contains example responses
    - Contains error handling pattern explanation
    - File structure shows tools.py
  </verify>
  <done>
    README.md updated with comprehensive tools documentation including decision framework, parameter tables, example responses, and error handling patterns
  </done>
</task>

</tasks>

<verification>
1. README.md renders correctly as markdown
2. Decision framework table is clear and accurate
3. Parameter tables match actual function signatures
4. Example responses match actual tool output format
5. File structure reflects current state (includes tools.py)
</verification>

<success_criteria>
- README includes Tools vs RAG decision framework
- Both tools documented with parameter tables
- Example responses provided
- Error handling pattern explained
- File structure updated to show tools.py
</success_criteria>

<output>
After completion, create `.planning/phases/02-function-calling-and-tools/02-03-SUMMARY.md`
</output>
