---
phase: 01-foundation-and-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workshop-materials/00-setup-verification.ipynb
autonomous: true

must_haves:
  truths:
    - "Participant can run verification notebook and see pass/fail status for all dependencies"
    - "Participant knows before workshop if their environment is properly configured"
    - "Authentication with GCP project succeeds in Colab"
    - "ADK 1.23.0 imports successfully"
    - "Gemini 2.5 Flash model responds to test prompt"
  artifacts:
    - path: "workshop-materials/00-setup-verification.ipynb"
      provides: "Pre-workshop environment validation notebook"
      min_lines: 80
  key_links:
    - from: "00-setup-verification.ipynb"
      to: "google.colab.auth"
      via: "authenticate_user call"
      pattern: "auth\\.authenticate_user"
    - from: "00-setup-verification.ipynb"
      to: "google.adk"
      via: "import and version check"
      pattern: "google\\.adk\\.__version__"
---

<objective>
Create the pre-workshop environment verification notebook that participants run 48 hours before the workshop.

Purpose: Catch authentication, dependency, and API access issues before workshop starts - preventing the common 40+ minute setup time sink.

Output: A complete Colab notebook (00-setup-verification.ipynb) with authentication, dependency verification, and Gemini model access tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verification notebook structure</name>
  <files>workshop-materials/00-setup-verification.ipynb</files>
  <action>
Create a Jupyter notebook (.ipynb format) with the following cells:

**Cell 1 (Markdown):** Title and instructions
- Title: "ADK Workshop - Environment Verification"
- Explain this notebook validates environment before workshop
- Instructions to run all cells and check for green checkmarks
- Note: Run this 48 hours before workshop

**Cell 2 (Code):** Project configuration
- Define PROJECT_ID variable (with placeholder "your-workshop-project")
- Define LOCATION variable (default "us-central1")
- Print configuration for user to verify

**Cell 3 (Code):** Colab authentication
- Import google.colab.auth
- Call auth.authenticate_user(project_id=PROJECT_ID)
- Set GOOGLE_CLOUD_PROJECT environment variable
- Print success message with checkmark emoji

**Cell 4 (Code):** Install ADK
- pip install google-adk==1.23.0 (quiet mode)
- Print success message

**Cell 5 (Code):** Verify Python and dependencies
- Check Python version >= 3.11
- Import google.adk and verify version is 1.23.0
- Import google.cloud.aiplatform
- Print results with pass/fail indicators

**Cell 6 (Code):** Verify GCP authentication
- Run gcloud auth list via subprocess
- Check for ACTIVE account
- Print authentication status

**Cell 7 (Code):** Verify Vertex AI API access
- Initialize aiplatform with project
- Print success or specific error message

**Cell 8 (Code):** Test Gemini model access
- Create minimal Agent with gemini-2.5-flash
- Call generate_content with "Hello"
- Verify response.text exists
- Print success with response preview

**Cell 9 (Code):** Summary verification function
- Define verify_environment() function that runs all checks
- Print consolidated pass/fail report
- Return True if all pass, False otherwise
- Include troubleshooting link placeholder

**Cell 10 (Markdown):** Next steps
- If all passed: "You're ready for the workshop!"
- If failed: Link to troubleshooting guide (placeholder URL)
- Contact information for help

Use ipynb JSON format with proper cell structure. Each code cell should have clear output expectations.
  </action>
  <verify>
python -c "import json; nb=json.load(open('workshop-materials/00-setup-verification.ipynb')); print(f'Cells: {len(nb[\"cells\"])}')" should show 10 cells
  </verify>
  <done>
Notebook has 10 cells covering authentication, ADK installation, dependency verification, API access tests, and Gemini model test with clear pass/fail indicators.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add comprehensive error handling and troubleshooting</name>
  <files>workshop-materials/00-setup-verification.ipynb</files>
  <action>
Enhance the verification notebook with robust error handling:

1. **Wrap each verification step in try/except:**
   - Catch specific exceptions (ImportError, subprocess.CalledProcessError, google.api_core.exceptions.*)
   - Print helpful error messages explaining what failed and why
   - Continue checking other items even if one fails

2. **Add specific troubleshooting guidance per failure:**
   - Python version wrong: "Install Python 3.11 or 3.12"
   - ADK import fails: "Run: pip install google-adk==1.23.0"
   - Auth fails: "Click the OAuth consent link above and authorize"
   - API not enabled: "Enable Vertex AI API at: https://console.cloud.google.com/apis/library/aiplatform.googleapis.com"
   - Gemini fails: "Check quota at: https://console.cloud.google.com/iam-admin/quotas"

3. **Add timeout handling:**
   - Set 30 second timeout for model calls
   - Handle TimeoutError with clear message about network issues

4. **Add final summary cell** with:
   - Count of passed/failed checks
   - List of failed items with fix actions
   - Clear "READY" or "NOT READY" status
  </action>
  <verify>
python -c "import json; nb=json.load(open('workshop-materials/00-setup-verification.ipynb')); src=''.join([c.get('source','') if isinstance(c.get('source',''),str) else ''.join(c.get('source',[])) for c in nb['cells']]); print('try/except found:', 'try:' in src); print('troubleshooting found:', 'troubleshoot' in src.lower() or 'Enable Vertex AI' in src)"
  </verify>
  <done>
Each verification step has try/except with specific error messages and troubleshooting guidance. Final summary shows clear ready/not ready status.
  </done>
</task>

</tasks>

<verification>
1. Notebook is valid JSON and opens in Jupyter/Colab
2. All 10 cells present with correct types (markdown/code)
3. Authentication cell uses google.colab.auth pattern
4. ADK version check verifies 1.23.0 specifically
5. Gemini test creates Agent and calls generate_content
6. Error handling provides actionable troubleshooting guidance
</verification>

<success_criteria>
- [ ] workshop-materials/00-setup-verification.ipynb exists and is valid JSON
- [ ] Notebook has authentication, dependency, and API access verification
- [ ] Each check has clear pass/fail indicator with emoji
- [ ] Failed checks provide specific troubleshooting guidance
- [ ] Final summary gives clear ready/not ready status
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-setup/01-01-SUMMARY.md`
</output>
